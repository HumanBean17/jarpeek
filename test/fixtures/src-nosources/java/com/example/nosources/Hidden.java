package com.example.nosources;

/** Fixture class shipped as a binary jar with no sources jar. */
public class Hidden {

    /** Returns the secret payload. */
    public String secret() {
        return "s3cret";
    }
}
